Train Topology
==============
Train topology implementation

Instructions
-----------
1. Clone the repo
3. run `npm i -g webpack webpack-dev-server`
2. run `npm install`
3. run `npm run serve`

To build run `npm run build`
